# =============================================================================
# SRC .htaccess - Tam Erişim Engeli
# =============================================================================
# Bu dizin web erişimine tamamen kapalıdır.
# =============================================================================

# Tüm erişimi engelle
<IfModule mod_authz_core.c>
    Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
    Order deny,allow
    Deny from all
</IfModule>
